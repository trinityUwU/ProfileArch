#!/usr/bin/env bash
# Généré automatiquement par WPE Manager (server.py) quand tu appliques des fonds.
# Ne pas éditer à la main — utilise l’interface http://localhost:6969

pkill -f -9 mpvpaper 2>/dev/null || true
pkill -f -9 linux-wallpaperengine 2>/dev/null || true
pkill -f -9 wpe_web_wallpaper 2>/dev/null || true
sleep 1
