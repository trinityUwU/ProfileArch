## █▄▀ █▀▀ █▄█ █▄▄ █ █▄░█ █▀▄ █ █▄░█ █▀▀ █▀
## █░█ ██▄ ░█░ █▄█ █ █░▀█ █▄▀ █ █░▀█ █▄█ ▄█

# see https://wiki.hyprland.org/configuring/keywords/ for more
# example binds, see https://wiki.hyprland.org/configuring/binds/ for more

#? ------- KEYBINDS_HYPRLAND_V_0_53 COMPATIBLE ----------------
#   This Config is compatible with keybinding changes in Hyprland v0.53+
#   To ensure your system is working we will not disable this block
#   User template files will be provided in $XDG_DATA_HOME/hyde/templates/hypr/keybindings.conf for manual update.

#? ------- Grouping of binds for easier management ----------------
#  $d=[Group Name|Subgroup Name1|Subgroup Name2|...]
# '$d' is a variable that is used to group binds together (or use another variable)
# This is only for organization purposes and is not a defined hyprland variable
# What we did here is to modify the Description of the binds to include the group name
# The $d will be parsed as a separate key to be use for a GUI or something pretty
# [Main|Subgroup1|Subgroup2|...]
# Main - The main groupname
# Subgroup1.. - The subgroup names can be use to avoid repeating the same description

#? ------- Variables ----------------
# Default if commented out
# $mainMod = Super # super / meta / windows key
# Assign apps
# $TERMINAL = kitty
# $EDITOR = code
# $EXPLORER = dolphin
# $BROWSER = firefox

$wm=Window Management
$d=[$wm]
bind = $mainMod, Q, $d close focused window, exec, hyde-shell dontkillsteam
bind = Alt, F4, $d close focused window, exec, hyde-shell dontkillsteam
bind = $mainMod, Delete, $d kill hyprland session, exec, hyde-shell logout
bind = $mainMod, W, $d Toggle floating, togglefloating
bind = $mainMod, G, $d toggle group, togglegroup
bind = Shift, F11, $d toggle fullscreen, fullscreen
bind = $mainMod, L, $d lock screen, exec, hyde-shell lock-session
bind = $mainMod Shift, F, $d toggle pin on focused window, exec, hyde-shell windowpin
bind = Control Alt, Delete, $d logout menu, exec, hyde-shell logoutlaunch
bind = Alt_R, Control_R, $d toggle waybar and reload config, exec, hyde-shell waybar --hide
# bindd = ALT_R, Control_R,toggle waybar, exec, killall waybar || waybar # toggle waybar without reloading, this is faster

$d=[$wm|Group Navigation]
bind = $mainMod Control, H, $d change active group backwards   , changegroupactive, b
bind = $mainMod Control, L, $d change active group forwards  , changegroupactive, f

$d=[$wm|Change focus]
bind = $mainMod, Left, $d focus left, movefocus, l
bind = $mainMod, Right, $d focus right , movefocus, r
bind = $mainMod, Up, $d focus up , movefocus, u
bind = $mainMod, Down , $d focus down, movefocus, d
bind = ALT, Tab,$d Cycle focus, cyclenext

$d=[$wm|Resize Active Window]
# Resize windows
binde = $mainMod Shift, Right, $d resize window right , resizeactive, 30 0
binde = $mainMod Shift, Left, $d resize window left, resizeactive, -30 0
binde = $mainMod Shift, Up, $d resize window up, resizeactive, 0 -30
binde = $mainMod Shift, Down, $d resize  window down, resizeactive, 0 30

# Move active window around current workspace with mainMod + Shift + Control [←→↑↓]
$d=[$wm|Move active window across workspace]
$moveactivewindow=grep -q "true" <<< $(hyprctl activewindow -j | jq -r .floating) && hyprctl dispatch moveactive
binde = $mainMod Shift Control, left, Move active window to the left, exec, $moveactivewindow -30 0 || hyprctl dispatch movewindow l
binde = $mainMod Shift Control, right, Move active window to the right, exec, $moveactivewindow 30 0 || hyprctl dispatch movewindow r
binde = $mainMod Shift Control, up, Move active window up, exec, $moveactivewindow  0 -30 || hyprctl dispatch movewindow u
binde = $mainMod Shift Control, down, Move active window down, exec, $moveactivewindow 0 30 || hyprctl dispatch movewindow d

# Move/Resize focused window
$d=[$wm|Move & Resize with mouse]
bindm = $mainMod, mouse:272, $d hold to move window, movewindow
bindm = $mainMod, mouse:273, $d hold to resize window, resizewindow
bindm = $mainMod, Z, $d hold to move window , movewindow
bindm = $mainMod, X, $d hold to resize window, resizewindow

# Toggle focused window split
$d=[$wm]
bind = $mainMod, J, $d toggle split, togglesplit

$l=Launcher
$d=[$l|Apps]
bind = $mainMod, T, $d terminal emulator , exec, $TERMINAL
bind = $mainMod Alt, T, $d dropdown terminal , exec, hyde-shell pypr toggle console
bind = $mainMod, E, $d file explorer , exec, $EXPLORER
bind = $mainMod, C, $d text editor , exec, $EDITOR
bind = $mainMod, B, $d web browser , exec, $BROWSER
bind = Control Shift, Escape, $d system monitor , exec, hyde-shell system.monitor

$d=[$l|Rofi menus]
$rofi-launch=hyde-shell rofilaunch
bind = $mainMod, A, $d application finder , exec, pkill -x rofi || $rofi-launch d
bind = $mainMod, TAB, $d window switcher , exec, pkill -x rofi || $rofi-launch w
bind = $mainMod Shift, E, $d file finder , exec, pkill -x rofi || $rofi-launch f
bind = $mainMod, slash, $d keybindings hint, exec, pkill -x rofi || hyde-shell keybinds_hint c # launch keybinds hint
bind = $mainMod, comma, $d emoji  picker , exec, pkill -x rofi || hyde-shell emoji-picker # launch emoji picker
bind = $mainMod, period, $d glyph picker , exec, pkill -x rofi || hyde-shell glyph-picker # launch glyph picker
bind = $mainMod, V, $d clipboard ,  exec, pkill -x rofi || hyde-shell cliphist -c # launch clipboard,
bind = $mainMod Shift, V, $d clipboard manager , exec, pkill -x rofi || hyde-shell cliphist # launch clipboard Manager
bind = $mainMod Shift, A, $d select rofi launcher , exec, pkill -x rofi || hyde-shell rofiselect # launch select menu


$hc=Hardware Controls
$d=[$hc|Audio]
binddl  = , F10, $d toggle mute output , exec, hyde-shell volumecontrol -o m # toggle audio mute
binddl  = , XF86AudioMute,$d  toggle mute output, exec, hyde-shell volumecontrol -o m # toggle audio mute
bindel = , F11, $d decrease volume , exec, hyde-shell volumecontrol -o d # decrease volume
bindel = , F12, $d increase volume , exec, hyde-shell volumecontrol -o i # increase volume
binddl  = , XF86AudioMicMute,$d un/mute microphone  , exec, hyde-shell volumecontrol -i m # toggle microphone mute
bindel = , XF86AudioLowerVolume, $d decrease volume , exec, hyde-shell volumecontrol -o d # decrease volume
bindel = , XF86AudioRaiseVolume, $d increase volume , exec, hyde-shell volumecontrol -o i # increase volume

$d=[$hc|Media]
binddl  = , XF86AudioPlay,$d play media, exec, playerctl play-pause # toggle between media play and pause
binddl  = , XF86AudioPause,$d pause media, exec, playerctl play-pause # toggle between media play and pause
binddl  = , XF86AudioNext,$d next media  , exec, playerctl next # media next
binddl  = , XF86AudioPrev,$d  previous media , exec, playerctl previous # media previous

$d=[$hc|Brightness]
bindel = , XF86MonBrightnessUp, $d increase brightness , exec, hyde-shell brightnesscontrol i # increase brightness
bindel = , XF86MonBrightnessDown, $d decrease brightness , exec, hyde-shell brightnesscontrol d # decrease brightness


$ut=Utilities
$d=[$ut]
bindl = $mainMod, K, $d toggle keyboard layout , exec, hyde-shell keyboardswitch # switch keyboard layout
bind = $mainMod Alt, G, $d game mode , exec, hyde-shell gamemode # disable hypr effects for gamemode
bind = $mainMod Shift, G, $d open game launcher , exec, hyde-shell gamelauncher # run game launcher for steam and lutris

$d=[$ut|Screen Capture]
bind = $mainMod Shift, P, $d color picker, exec, hyprpicker -an # Pick color (Hex) >> clipboard#
bind = $mainMod, P, $d snip screen , exec, hyde-shell screenshot s # partial screenshot capture
bind = $mainMod Control, P, $d freeze and snip screen, exec, hyde-shell screenshot sf # partial screenshot capture (frozen screen)
bindl = $mainMod Alt, P, $d print monitor , exec, hyde-shell screenshot m # monitor screenshot capture
bindl = , Print, $d print all monitors , exec, hyde-shell screenshot p # all monitors screenshot capture

# TODO Make a main rofi menu for these selectors
$rice=Theming and Wallpaper
$d=[$rice]
bind = $mainMod Alt, Right, $d next global wallpaper , exec, hyde-shell wallpaper -Gn # next global wallpaper
bind = $mainMod Alt, Left, $d previous global wallpaper , exec, hyde-shell wallpaper -Gp # previous global wallpaper
bind = $mainMod Shift, W, $d select a global wallpaper , exec, pkill -x rofi || hyde-shell wallpaper -SG # launch wallpaper select menu
bind = $mainMod Alt, Up, $d next waybar layout , exec, hyde-shell wbarconfgen n # next waybar mode
bind = $mainMod Alt, Down, $d previous waybar layout , exec, hyde-shell wbarconfgen p # previous waybar mode
bind = $mainMod Shift, R, $d wallbash mode selector , exec, pkill -x rofi || hyde-shell wallbashtoggle -m # launch wallbash mode select menu
bind = $mainMod Shift, T, $d select a theme, exec, pkill -x rofi || hyde-shell themeselect # launch theme select menu
bind = $mainMod+Shift, Y, $d select animations, exec, pkill -x rofi || hyde-shell animations --select # launch animations select menu
bind = $mainMod+Shift, U, $d select hyprlock layout, exec, pkill -x rofi || hyde-shell hyprlock --select # launch hyprlock layout select menu



$ws=Workspaces
$d=[$ws|Navigation]
bind = $mainMod, 1, $d navigate to workspace 1 , workspace, 1
bind = $mainMod, 2, $d navigate to workspace 2 , workspace, 2
bind = $mainMod, 3, $d navigate to workspace 3 , workspace, 3
bind = $mainMod, 4, $d navigate to workspace 4 , workspace, 4
bind = $mainMod, 5, $d navigate to workspace 5 , workspace, 5
bind = $mainMod, 6, $d navigate to workspace 6 , workspace, 6
bind = $mainMod, 7, $d navigate to workspace 7 , workspace, 7
bind = $mainMod, 8, $d navigate to workspace 8 , workspace, 8
bind = $mainMod, 9, $d navigate to workspace 9 , workspace, 9
bind = $mainMod, 0, $d navigate to workspace 10 , workspace, 10

$d=[$ws|Navigation|Relative workspace]
bind = $mainMod Control, Right, $d change active workspace forwards  , workspace, r+1
bind = $mainMod Control, Left, $d change active workspace backwards , workspace, r-1

$d=[$ws|Navigation]
bind = $mainMod Control, Down, $d navigate to the nearest empty workspace , workspace, empty

# Move focused window to a workspace
$d=[$ws|Move window to workspace]
bind = $mainMod Shift, 1, $d move to workspace 1 , movetoworkspace, 1
bind = $mainMod Shift, 2, $d move to workspace 2 , movetoworkspace, 2
bind = $mainMod Shift, 3, $d move to workspace 3 , movetoworkspace, 3
bind = $mainMod Shift, 4, $d move to workspace 4 , movetoworkspace, 4
bind = $mainMod Shift, 5, $d move to workspace 5 , movetoworkspace, 5
bind = $mainMod Shift, 6, $d move to workspace 6 , movetoworkspace, 6
bind = $mainMod Shift, 7, $d move to workspace 7 , movetoworkspace, 7
bind = $mainMod Shift, 8, $d move to workspace 8 , movetoworkspace, 8
bind = $mainMod Shift, 9, $d move to workspace 9 , movetoworkspace, 9
bind = $mainMod Shift, 0, $d move to workspace 10 , movetoworkspace, 10

# Move focused window to a relative workspace
$d=[$ws]
bind = $mainMod Control+Alt, Right, $d move window to next relative workspace , movetoworkspace, r+1
bind = $mainMod Control+Alt, Left, $d move window to previous relative workspace , movetoworkspace, r-1


# Scroll through existing workspaces
$d=[$ws|Navigation]
bind = $mainMod, mouse_down, $d next workspace, workspace, e+1
bind = $mainMod, mouse_up, $d previous workspace, workspace, e-1


# Move/Switch to special workspace (scratchpad)
$d=[$ws|Navigation|Special workspace]
bind = $mainMod Shift, S, $d move to scratchpad  , movetoworkspace, special
bind = $mainMod Alt, S, $d move to scratchpad (silent) , movetoworkspacesilent, special
bind = $mainMod, S, $d toggle scratchpad ,  togglespecialworkspace

# Move focused window to a workspace silently
$d=[$ws|Navigation|Move window silently]
bind = $mainMod Alt, 1, $d move to workspace 1  (silent), movetoworkspacesilent, 1
bind = $mainMod Alt, 2, $d move to workspace 2  (silent), movetoworkspacesilent, 2
bind = $mainMod Alt, 3, $d move to workspace 3  (silent), movetoworkspacesilent, 3
bind = $mainMod Alt, 4, $d move to workspace 4  (silent), movetoworkspacesilent, 4
bind = $mainMod Alt, 5, $d move to workspace 5  (silent), movetoworkspacesilent, 5
bind = $mainMod Alt, 6, $d move to workspace 6  (silent), movetoworkspacesilent, 6
bind = $mainMod Alt, 7, $d move to workspace 7  (silent), movetoworkspacesilent, 7
bind = $mainMod Alt, 8, $d move to workspace 8  (silent), movetoworkspacesilent, 8
bind = $mainMod Alt, 9, $d move to workspace 9  (silent), movetoworkspacesilent, 9
bind = $mainMod Alt, 0, $d move to workspace 10 (silent), movetoworkspacesilent, 10



$d=#! unset the group name
